# The only committed env file. Copy to .env (gitignored) and fill in real values.
# Never commit .env. Client-side code gets public/anon keys only; server secrets stay server-side.

# EXAMPLE_API_KEY=
# DATABASE_URL=
