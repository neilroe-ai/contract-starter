# Secrets — never commit (only .env.example is tracked)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*serviceAccount*.json

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# JS/TS
node_modules/
dist/

# Run artifacts, logs, scratch — live OUTSIDE the repo; never staged
*.log
tmp/
scratch/
